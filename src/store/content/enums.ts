export enum BlockTypes {
  client_name = "client_name",
  client_email = "client_email",
  client_data = "client_data",
  visual_data = "visual_data",
  tabs = "tabs",
}

export enum VisualBlockTypes {
  accordion = "accordion",
  bricks = "bricks",
}
